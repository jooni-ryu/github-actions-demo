name: Hello Github Actions
on: [push]

jobs:
  greet:
      runs-on: ubuntu-latest
      steps:
        - name: Say hello
          run: echo "Hello from Gitghub Actions!"
        - run: echo "I'll list files."
        - run: ls -al
